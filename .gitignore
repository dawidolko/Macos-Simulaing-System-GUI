*.log
.DS_Store
.cache/
node_modules/
dist/

# Build output. The root index.html is the Vite *template*; a compiled bundle
# committed here is what stopped `vite build` from ever compiling src/ again.
/assets
/dist
